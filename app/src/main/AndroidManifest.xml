<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.medtech.medtech">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Poison" />
        <activity android:name=".shock" />
        <activity android:name=".stroke" />
        <activity android:name=".fractures" />
        <activity android:name=".allergies" />
        <activity android:name=".heartattack" />
        <activity android:name=".electrickshock" />
        <activity android:name=".burns" />
        <activity android:name=".drowning" />
        <activity android:name=".bleeding"></activity>
    </application>

</manifest>